# Program-to-riverse-string
#include<iostream.h>
#include<conio.h>
#include<string.h>
void main(){
clrscr();
char str[100];
int len, l;
cout<<"enter a string:";
cin>>str;
len=strlen(str);
cout<<"reversed strings":
for(l=len-1:l>=0;l--){
cout<<str[1];
}
getch();
}
